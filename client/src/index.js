import React from "react";
import ReactDOM from "react-dom/client"; // react-dom/client available in v18+
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
